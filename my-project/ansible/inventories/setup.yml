all:
  vars:
    ansible_user: admin
    ansible_ssh_private_key_file: /home/asus/takima.key
    ansible_python_interpreter: /opt/docker_venv/bin/python3
  children:
    prod:
      hosts:
        oscar.hunaut.takima.school
